import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import styled from 'styled-components'
import { faRotate } from '@fortawesome/free-solid-svg-icons'

export const IconRotate = styled(FontAwesomeIcon).attrs({
  icon: faRotate
})``
